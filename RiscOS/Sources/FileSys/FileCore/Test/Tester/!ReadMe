This file describes what this tester does:

Purpose of Tester:

To test as many filing system operations as possible.

Method:

Random selection of operation based on a repeatable random number generator.

Use:

Tester <result file> <wrap length> <test dir>[ <test dir>]*

result file: log of the last <wrap length> bytes of log output. END is where
the log file got to.

test dir: A dir where tests get performed in.

test dirs must be given as fully canonicalised names