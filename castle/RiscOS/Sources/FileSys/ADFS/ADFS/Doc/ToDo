To be done:
-----------


Done:
-----

Fix ADFS_VetFormat to adjust the track/track skew to ensure non-sector
  overrun on track step (no embarrasing pause as the head steps). Solution
  chosen: If interleave and both skews are 0 and the gap1s are the same
  then set track/track skew to 1.
Fix *Format for MultiFS scheme
Fix ADFS_VetFormat to respond to 710 machine Id.
Add *Help MultiFS Format
Remove TrackImage as its no longer used
Internationalise it.
Don't bother increasing disc changed number on format - *Dismount doe the business anyway.
Improve *Format confirm string to include format and disc name.
Fix E format defaulting in *format.
Check whether escape whilst formatting fix wants re-installing
