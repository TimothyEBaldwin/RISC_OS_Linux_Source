The Edit application allows you to create or edit
files of plain text (like this one), and to create
and edit BASIC programs.

This help file is an example of an Edit document.
Click the pointer inside this window to set the
type-in point (the 'caret'), then type.
