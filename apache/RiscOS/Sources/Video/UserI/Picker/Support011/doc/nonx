nonx.c
------

   Experimental module to enable code to be written that goes

      wimp_set_icon_state (w, i, x, y);

instead of

      if ((error = xwimp_set_icon_state (w, i, x, y)) != NULL)
         goto finish;

but still with full error checking. Not complete yet, though.
